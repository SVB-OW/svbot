name: 'Code Style'

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:
  format:
    runs-on: self-hosted

    steps:
      - run: yarn format

  lint:
    runs-on: self-hosted

    steps:
      - run: yarn lint
